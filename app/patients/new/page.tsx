"use client";import{useState}from"react";import{useRouter}from"next/navigation";export default function NewPatient(){const r=useRouter();const[f,setF]=useState({patientNo:"",firstName:"",lastName:"",sex:"",dateOfBirth:"",phone:"",address:""});const set=(k:string,v:string)=>setF({...f,[k]:v});async function submit(e:React.FormEvent){e.preventDefault();const x=await fetch("/api/patients",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)});if(x.ok)r.push("/patients")}return <div className="card"><h1>Register Patient</h1><form className="form" onSubmit={submit}>{Object.entries(f).map(([k,v])=><input className="input" key={k} placeholder={k} value={v} onChange={e=>set(k,e.target.value)} required={["patientNo","firstName","lastName"].includes(k)}/>)}<button className="btn">Save patient</button></form></div>}